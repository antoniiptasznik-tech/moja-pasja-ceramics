---
interface Props {
  class?: string;
  id?: string;
}

const { class: className, id } = Astro.props;
---

<section id={id} class={`relative px-6 py-24 md:py-32 ${className || ''}`}>
  <div class="mx-auto max-w-7xl w-full">
    <slot />
  </div>
</section>
